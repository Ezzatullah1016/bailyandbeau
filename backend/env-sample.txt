# Database (use placeholders — never commit real passwords)
DATABASE_URL=postgres://postgres:<RDS_PASSWORD>@<RDS_ENDPOINT>:5432/<RDS_DB_NAME>

# S3 / Storage (bucket / region only; IAM keys belong in environment secrets)
AWS_STORAGE_BUCKET_NAME=<bucket-name>
AWS_S3_REGION_NAME=eu-north-1
AWS_S3_CUSTOM_DOMAIN=<bucket-name>.s3.<region>.amazonaws.com
USE_S3=True

# Django
ALLOWED_HOSTS=<EC2_PUBLIC_IP>,<YOUR_DOMAIN>,localhost,127.0.0.1
DEBUG=False
SECRET_KEY=<generate-new-strong-key>
# Must include every browser origin that calls the API (Next.js dev, prod HTTPS IP/domain).
CORS_ALLOWED_ORIGINS=https://<EC2_PUBLIC_IP>,http://localhost:3000

# Frontend build on EC2 / Amplify (Next.js): point browser calls at Django API
# NEXT_PUBLIC_API_BASE_URL=https://<EC2_PUBLIC_IP>/api/v1

# Livekit (already set on staging)
LIVEKIT_API_KEY=<your-key>
LIVEKIT_API_SECRET=<your-secret>
LIVEKIT_URL=<your-wss-url>