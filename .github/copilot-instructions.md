# Workspace Setup Status

- [x] Verify that the `copilot-instructions.md` file in the `.github` directory is created.
- [x] Clarify Project Requirements — Python Django starter project.
- [x] Scaffold the Project — Created the Django project `config` and app `core` in the current workspace.
- [x] Customize the Project — Added a working home page and root URL.
- [x] Install Required Extensions — No additional extensions were required.
- [x] Compile the Project — Verified with `manage.py test` and applied migrations successfully.
- [x] Create and Run Task — Added the `Run Django Server` task.
- [x] Launch the Project — Local development server launched and verified on request.
- [x] Ensure Documentation is Complete — `README.md` and this file are up to date.

## Project Notes

- Start the server with the `Run Django Server` task or `& ".\.venv\Scripts\python.exe" manage.py runserver`
- Open `http://127.0.0.1:8000/`
- Super admin dashboard: `http://127.0.0.1:8000/admin/` (redirects from `/admin/`). Stock Django admin: `http://127.0.0.1:8000/django-admin/`
